# DEPRECATED: Use values-scenario4.yaml instead
# Scenario S4: Connection reset scenario
# Tests behavior when gRPC connection to B is abruptly reset

appB:
  env:
    - name: B_DELAY_MS
      value: "5"  # Normal delay

# Fault injection parameters (for scenario runner):
# target: single A pod
# method: iptables -A OUTPUT -p tcp --dport 50051 -j REJECT --reject-with tcp-reset
# timing: t=15s (inject), t=30s (remove)
# duration: 15s

# Channel pool configuration (for resilient mode):
# CHANNEL_POOL_SIZE: 1 (baseline) or 4 (resilient)
